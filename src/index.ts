/**
 * This function does absolutely nothing. Like, seriously.
 */
export function doNothing() {
    // Deep thoughts and contemplation.
}

/**
 * This function achieves absolutely nothing. It's quite an achiever in doing so.
 */
export function achieveNothing() {
    const aspiration = "achieve something";
    const reality = aspiration.substr(0, 0); // Empty it out
    return reality; // Yep, it's nothing.
}

/**
 * A function that loops for no reason whatsoever.
 */
export function pointlessLoop() {
    for (let i = 0; i < 10; i++) {
        // On second thought, let's not loop here. Tis a silly place.
    }
}
